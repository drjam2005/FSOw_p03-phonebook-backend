{
	"name": "phonebook_backend",
	"version": "0.0.1",
	"description": "phonebook backend",
	"license": "MIT",
	"author": "drjam2005",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
		"deploy:full": "npm run build:ui && git add . && git commit && git push",
		"start": "node index.js",
		"build:start": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend && cd ../backend/ && node index.js"
	},
	"dependencies": {
		"dotenv": "^17.4.2",
		"express": "^5.2.1",
		"mongoose": "^9.7.4",
		"morgan": "^1.11.0"
	}
}
